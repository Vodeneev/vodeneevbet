# Arbitrage Finder - Система поиска арбитражных ситуаций

Система для автоматического поиска арбитражных ситуаций (арбов) в букмекерских конторах с использованием микросервисной архитектуры на Go.

## 🏗️ Архитектура

### Высокоуровневая схема
```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Parser    │    │ Calculator  │    │    API      │
│  Service    │───▶│  Service    │───▶│  Service    │
│             │    │             │    │             │
└─────────────┘    └─────────────┘    └─────────────┘
       │                   │                   │
       ▼                   ▼                   ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│    YDB      │    │ PostgreSQL  │    │  Frontend   │
│ (Cloud DB)  │    │ (History)   │    │   (Web)     │
└─────────────┘    └─────────────┘    └─────────────┘
```

### Компоненты системы

#### 1. **Parser Service** (`internal/parser/`)
- **Назначение**: Парсинг коэффициентов с сайтов букмекеров
- **Технологии**: Go, HTTP клиенты, HTML парсинг
- **Хранилище**: YDB (Yandex Database) для быстрого доступа
- **Особенности**: 
  - Асинхронный парсинг с интервалами
  - Обработка ошибок и retry логика
  - Поддержка множественных букмекеров

#### 2. **Calculator Service** (`internal/calculator/`)
- **Назначение**: Поиск арбитражных ситуаций
- **Алгоритм**: Анализ комбинаций коэффициентов между БК
- **Критерии**: Минимальный процент прибыли, максимальные комбинации
- **Результат**: Список найденных арбов с расчетами

#### 3. **API Service** (`internal/api/`)
- **Назначение**: REST API для доступа к данным
- **Эндпоинты**: 
  - `GET /api/arbs` - список арбитражей
  - `GET /api/odds` - текущие коэффициенты
  - `GET /api/matches` - доступные матчи
- **Формат**: JSON API

#### 4. **Storage Layer** (`internal/pkg/storage/`)
- **YDB**: Основное хранилище для коэффициентов (быстрый доступ)
- **PostgreSQL**: История арбитражей и метаданные
- **Интерфейсы**: Абстракция для легкого переключения хранилищ

## 🛠️ Технологический стек

### Backend
- **Язык**: Go 1.22+
- **Фреймворки**: Стандартная библиотека Go
- **Базы данных**: 
  - YDB (Yandex Database) - для коэффициентов
  - PostgreSQL - для истории
- **Контейнеризация**: Docker, Docker Compose

### Frontend
- **Планируется**: React/Vue.js + TypeScript
- **Функции**: Отображение арбов, фильтрация, уведомления

### Инфраструктура
- **Облако**: Yandex Cloud
- **База данных**: YDB Serverless
- **Контейнеры**: Docker
- **Мониторинг**: Встроенные логи Go

## 📁 Структура проекта

```
vodeneevbet/
├── internal/
│   ├── parser/          # Парсер букмекеров
│   ├── calculator/      # Калькулятор арбитражей
│   ├── api/             # REST API сервер
│   └── pkg/             # Общие библиотеки
│       ├── models/      # Модели данных
│       ├── config/      # Конфигурация
│       └── storage/     # Слой хранения
├── configs/             # Конфигурационные файлы
├── deployments/         # Docker файлы
├── keys/               # Ключи аутентификации
└── docker-compose.yml   # Локальная инфраструктура
```

## 🚀 Быстрый старт

### 1. Настройка окружения
```bash
# Клонирование репозитория
git clone <repository-url>
cd vodeneevbet

# Установка зависимостей
go mod tidy
```

### 2. Настройка YDB
1. Создайте Service Account в Yandex Cloud
2. Назначьте роли: `YDB Database User`, `YDB Database Viewer`
3. Создайте ключ аутентификации
4. Сохраните ключ в `keys/service-account-key.json`

### 3. Запуск инфраструктуры
```bash
# Запуск PostgreSQL и Adminer
docker-compose up -d

# Проверка статуса
docker ps
```

### 4. Запуск парсера
```bash
# Запуск парсера
cd internal/parser
go run main.go

# Или с кастомной конфигурацией
go run main.go -config ../../configs/local.yaml
```

## ⚙️ Конфигурация

### Основные параметры (`configs/local.yaml`)
```yaml
# YDB конфигурация
ydb:
  endpoint: "grpcs://ydb.serverless.yandexcloud.net:2135"
  database: "/ru-central1/your-database-id"
  service_account_key_file: "keys/service-account-key.json"

# PostgreSQL для истории
postgres:
  dsn: "host=localhost user=arb_user password=arb_password dbname=arb_db port=5432 sslmode=disable"

# Настройки парсера
parser:
  interval: 30s
  user_agent: "Mozilla/5.0..."
  timeout: 30s

# Настройки калькулятора
calculator:
  min_profit_percent: 1.0
  max_combinations: 1000
```

## 📊 Модели данных

### Odd (Коэффициент)
```go
type Odd struct {
    MatchID    string             `json:"match_id"`
    Bookmaker  string             `json:"bookmaker"`
    Market     string             `json:"market"`     // "1x2", "total", "handicap"
    Outcomes   map[string]float64 `json:"outcomes"`   // {"win_home": 1.5, "draw": 3.0}
    UpdatedAt  time.Time          `json:"updated_at"`
    MatchName  string             `json:"match_name"`
    MatchTime  time.Time          `json:"match_time"`
    Sport      string             `json:"sport"`
}
```

### Arbitrage (Арбитраж)
```go
type Arbitrage struct {
    ID            string    `json:"id"`
    MatchID       string    `json:"match_id"`
    MatchName     string    `json:"match_name"`
    ProfitPercent float64   `json:"profit_percent"`
    Bets          []Bet     `json:"bets"`
    FoundAt       time.Time `json:"found_at"`
}
```

## 🔄 Workflow

1. **Парсинг**: Парсеры собирают коэффициенты с БК каждые 30 секунд
2. **Хранение**: Данные сохраняются в YDB для быстрого доступа
3. **Анализ**: Калькулятор ищет арбитражные ситуации
4. **Сохранение**: Найденные арбы сохраняются в PostgreSQL
5. **API**: REST API предоставляет доступ к данным
6. **Frontend**: Веб-интерфейс отображает результаты

## 🧪 Тестирование

### Запуск тестов
```bash
# Тест YDB подключения
go run test_ydb_full.go

# Тест парсера
cd internal/parser
go run main.go
```

### Проверка данных
```bash
# Подключение к PostgreSQL
docker exec -it arb-postgres psql -U arb_user -d arb_db

# Просмотр арбитражей
SELECT * FROM arbitrages ORDER BY found_at DESC LIMIT 10;
```

## 🚧 Roadmap

### Этап 1: MVP ✅
- [x] Базовая структура проекта
- [x] Парсер с тестовыми данными
- [x] YDB интеграция
- [x] Docker инфраструктура

### Этап 2: Калькулятор (В процессе)
- [ ] Алгоритм поиска арбитражей
- [ ] Оптимизация производительности
- [ ] Фильтрация по критериям

### Этап 3: API и Frontend
- [ ] REST API сервер
- [ ] Веб-интерфейс
- [ ] Real-time уведомления

### Этап 4: Продакшен
- [ ] Мониторинг и логирование
- [ ] Масштабирование
- [ ] Безопасность

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

Для вопросов и предложений создавайте Issues в репозитории.