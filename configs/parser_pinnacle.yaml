postgres:
  dsn: "host=localhost user=arb_user password=arb_password dbname=arb_db port=5432 sslmode=disable"

parser:
  interval: 60s
  user_agent: "ValueBetBot/1.0 (https://github.com/Vodeneev/vodeneevbet)"
  timeout: 30s
  headers: {}
  pinnacle:
    base_url: "https://guest.api.arcadia.pinnacle.com"
    api_key: ""        # optionally via ENV PINNACLE_API_KEY
    device_uuid: ""    # optionally via ENV PINNACLE_DEVICE_UUID
    matchup_ids: []    # empty => auto-discovery mode

value_calculator:
  sports: ["football"]
