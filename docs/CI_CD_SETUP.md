# Настройка CI/CD для автоматического деплоя

Автоматический деплой через GitHub Actions или GitLab CI позволяет автоматически развертывать сервисы на VM при каждом пуше в репозиторий.

## 🎯 GitHub Actions

### Настройка

1. **Создайте SSH ключ для деплоя:**

```bash
# На локальной машине или на VM
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy
```

2. **Добавьте публичный ключ на обе VM:**

```bash
# На vm-parsers
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub vodeneevm@158.160.197.172

# На vm-core-services
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub vodeneevm@158.160.200.253
```

3. **Добавьте приватный ключ в GitHub Secrets:**

   - Перейдите в репозиторий на GitHub
   - Settings → Secrets and variables → Actions
   - Нажмите "New repository secret"
   - Имя: `SSH_PRIVATE_KEY`
   - Значение: содержимое файла `~/.ssh/github_actions_deploy` (приватный ключ)

4. **Добавьте адреса VM в GitHub Secrets (обязательно для текущего workflow):**

   - Settings → Secrets and variables → Actions → "New repository secret"
   - Имя: `VM_PARSERS_HOST` — значение: IP/DNS vm-parsers (например, `158.160.197.172`)
   - Имя: `VM_CORE_HOST` — значение: IP/DNS vm-core-services (например, `158.160.200.253`)

5. *(Опционально)* **Переопределите пользователя SSH:**

   - Имя: `VM_USER` — значение: имя пользователя на VM (по умолчанию workflow использует `vodeneevm`)

6. **Проверьте, что workflow файл существует:**

   Файл `.github/workflows/deploy.yml` уже создан в проекте.

### Как это работает

- При пуше в ветку `main` или `master` автоматически запускается деплой
- Можно также запустить вручную через Actions → Deploy to VMs → Run workflow
- Деплой происходит параллельно на обе VM

### Настройка триггеров

По умолчанию деплой запускается при изменении:
- `internal/**` - код сервисов
- `configs/**` - конфигурация
- `go.mod`, `go.sum` - зависимости

Чтобы изменить триггеры, отредактируйте `.github/workflows/deploy.yml`:

```yaml
on:
  push:
    branches:
      - main
    paths:
      - 'internal/**'  # Измените пути по необходимости
```

## 🔷 GitLab CI

### Настройка

1. **Создайте SSH ключ для деплоя:**

```bash
ssh-keygen -t rsa -b 4096 -C "gitlab-ci-deploy" -f ~/.ssh/gitlab_ci_deploy
```

2. **Добавьте публичный ключ на обе VM:**

```bash
# На vm-parsers
ssh-copy-id -i ~/.ssh/gitlab_ci_deploy.pub vodeneevm@158.160.197.172

# На vm-core-services
ssh-copy-id -i ~/.ssh/gitlab_ci_deploy.pub vodeneevm@158.160.200.253
```

3. **Добавьте приватный ключ в GitLab CI/CD Variables:**

   - Перейдите в проект на GitLab
   - Settings → CI/CD → Variables
   - Нажмите "Add variable"
   - Key: `SSH_PRIVATE_KEY`
   - Value: содержимое файла `~/.ssh/gitlab_ci_deploy` (приватный ключ)
   - Отметьте "Mask variable" и "Protect variable"

4. **Проверьте, что `.gitlab-ci.yml` существует:**

   Файл `.gitlab-ci.yml` уже создан в корне проекта.

### Как это работает

- При пуше в ветку `main` или `master` автоматически запускается деплой
- Деплой происходит параллельно на обе VM
- Используется Docker runner (убедитесь, что он настроен в GitLab)

### Настройка GitLab Runner

Если у вас еще нет GitLab Runner:

1. Установите GitLab Runner на сервере или используйте shared runners
2. Зарегистрируйте runner с тегом `docker`
3. Или уберите `tags: - docker` из `.gitlab-ci.yml`, если используете другой runner

## 🔐 Безопасность

### Рекомендации

1. **Используйте отдельный SSH ключ** только для CI/CD, не ваш личный ключ
2. **Ограничьте права ключа** на VM (можно использовать `authorized_keys` с ограничениями)
3. **Регулярно ротируйте ключи** (меняйте раз в несколько месяцев)
4. **Не коммитьте приватные ключи** в репозиторий (они должны быть только в Secrets/Variables)

### Ограничение прав SSH ключа

На VM можно ограничить, что может делать ключ через `authorized_keys`:

```bash
# На VM, отредактируйте ~/.ssh/authorized_keys
# Добавьте перед ключом:
command="/bin/true",no-port-forwarding,no-X11-forwarding,no-pty ssh-rsa AAAAB3...
```

Или создайте отдельного пользователя только для деплоя с ограниченными правами.

## 🧪 Тестирование

### Тест GitHub Actions локально

Используйте [act](https://github.com/nektos/act):

```bash
# Установите act
# Затем запустите workflow локально
act push -s SSH_PRIVATE_KEY="$(cat ~/.ssh/github_actions_deploy)"
```

### Тест GitLab CI локально

Используйте [gitlab-runner](https://docs.gitlab.com/runner/):

```bash
gitlab-runner exec docker deploy-parsers
```

## 📊 Мониторинг деплоев

### GitHub Actions

- Перейдите в репозиторий → Actions
- Видите историю всех запусков
- Можно посмотреть логи каждого шага

### GitLab CI

- Перейдите в проект → CI/CD → Pipelines
- Видите историю всех запусков
- Можно посмотреть логи каждого job

## 🔄 Откат изменений

Если деплой прошел неудачно:

1. **Откатите коммит:**
```bash
git revert HEAD
git push
```

2. **Или задеплойте предыдущую версию вручную:**
```bash
git checkout <previous-commit>
make deploy-all
git checkout main
```

## ❓ Troubleshooting

### GitHub Actions: "Permission denied (publickey)"

- Убедитесь, что приватный ключ правильно добавлен в Secrets
- Проверьте, что публичный ключ добавлен на VM
- Проверьте формат ключа (должен начинаться с `-----BEGIN OPENSSH PRIVATE KEY-----`)

### GitLab CI: "Host key verification failed"

- Убедитесь, что `ssh-keyscan` правильно настроен в `.gitlab-ci.yml`
- Проверьте, что хост доступен из GitLab Runner

### Деплой падает с ошибкой сборки

- Проверьте, что на VM установлен Go нужной версии
- Проверьте логи в Actions/Pipelines
- Попробуйте запустить деплой вручную для диагностики

### Сервисы не запускаются после деплоя

- Проверьте логи на VM: `ssh vm-parsers "sudo journalctl -u vodeneevbet-parser -n 50"`
- Убедитесь, что конфигурация правильная
- Проверьте права на файлы

## 📝 Примеры использования

### Деплой только при изменении определенных файлов

В `.github/workflows/deploy.yml`:

```yaml
on:
  push:
    branches: [main]
    paths:
      - 'internal/parser/**'  # Только парсер
```

### Деплой в разные окружения

Создайте отдельные workflow для staging и production:

```yaml
# .github/workflows/deploy-staging.yml
on:
  push:
    branches: [develop]

# .github/workflows/deploy-production.yml
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy" to confirm'
        required: true
```

### Уведомления о деплое

Добавьте в workflow отправку уведомлений в Telegram/Slack:

```yaml
- name: Notify Telegram
  if: success()
  run: |
    curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
      -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
      -d "text=✅ Deploy successful!"
```
